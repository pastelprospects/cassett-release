Overview:
Enable desktop users to download Desktop app via the existing PWA install button (installPwaBtn). Mobile users keep the current PWA installation flow.

Detection: Desktop vs mobile; to show appropriate option.
Desktop: “Download Desktop App” with platform-specific installers (Windows/macOS/Linux).
Mobile: Existing PWA install.
